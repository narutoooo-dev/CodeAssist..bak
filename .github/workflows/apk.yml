name: APK

on:
  push:
    branches: [ main, master ]
  pull_request:
  workflow_dispatch:

env:
  JAVA_VERSION: "25"

concurrency:
  group: apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-debug:
    name: assembleDebug
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: "platform-tools platforms;android-36 platforms;android-36.1 build-tools;36.0.0"

      - name: Disable Kotlin incremental compilation (CI)
        run: |
          mkdir -p ~/.gradle
          echo "kotlin.incremental=false" >> ~/.gradle/gradle.properties

      - name: Build debug APK
        run: ./gradlew :ide-android:assembleDebug --console=plain --stacktrace

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: codeassist-debug-apk
          path: ide-android/build/outputs/apk/debug/*.apk
          if-no-files-found: error
          retention-days: 30

  build-release:
    name: assembleRelease
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: "platform-tools platforms;android-36 platforms;android-36.1 build-tools;36.0.0"

      - name: Disable Kotlin incremental compilation (CI)
        run: |
          mkdir -p ~/.gradle
          echo "kotlin.incremental=false" >> ~/.gradle/gradle.properties

      - name: Generate debug keystore for release
        run: |
          keytool -genkey -v -keystore release.keystore -alias release -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass android -keypass android -dname "CN=CI, OU=CI, O=CI, L=CI, ST=CI, C=CI"

      - name: Create keystore properties
        run: |
          cat > keystore.properties << EOF
          storeFile=release.keystore
          storePassword=android
          keyAlias=release
          keyPassword=android
          EOF

      - name: Build release APK
        run: ./gradlew :ide-android:assembleRelease --console=plain --stacktrace

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: codeassist-release-apk
          path: ide-android/build/outputs/apk/release/*.apk
          if-no-files-found: error
          retention-days: 30
